<li>
    <span class="orderItemBar">
        {{order_item}} 
    </span>

    <span class="statusChangeBtn">
        <button class="statusChange" data-id="{{id}}" data-newstatus="{{status}}">
            {{#if status}}Close Order{{else}}Reopen Order{{/if}}
        </button>
    </span>
</li>
